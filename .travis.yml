sudo: false

language:
  - python

python:
  - 3.7

install:
  - pip install -r requirements.txt

script:
  - pycodestyle mamba-controller
  - pytest --verbose --capture=no mamba-controller
  - pip install .

notifications:
  email: false
